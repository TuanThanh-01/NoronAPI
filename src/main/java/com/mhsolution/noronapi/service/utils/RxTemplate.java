package com.mhsolution.noronapi.service.utils;

import io.reactivex.rxjava3.core.Single;
import io.reactivex.rxjava3.schedulers.Schedulers;

public class RxTemplate {

//    public static <T> Single<T> rxSchedulerIo(SupplierThrowable<T> supplier) {
//        Object authentication = (noronSecurityProvider != null) ? noronSecurityProvider.getAuthentication() : null;
//        return Single.just("io")
//                .subscribeOn(Schedulers.io())
//                .flatMap(s -> rxBlockingAsync(supplier, authentication));
//    }

}
